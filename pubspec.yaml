name: ai_assistant
description: "AI Assistant Application."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  http: ^1.2.2
  google_fonts: ^6.2.1
  flutter_lints: ^5.0.0
  google_generative_ai: ^0.4.6

flutter:

  uses-material-design: true

  assets:
    - assets/services logo/Gemini.png
